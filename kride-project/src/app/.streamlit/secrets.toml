# Streamlit Cloud 배포 시 이 파일에 시크릿을 설정합니다.
# 로컬에서도 사용 가능합니다.
#
# ⚠️ 이 파일은 절대 GitHub에 올리지 마세요! (.gitignore에 이미 포함됨)
#
# [ Streamlit Cloud 설정 방법 ]
# 1. https://share.streamlit.io 로그인
# 2. 앱 설정 → Secrets 섹션에 아래 내용을 붙여넣기

# ── KMA 날씨 API ──────────────────────────────────────────────
KMA_API_KEY = "여기에_기상청_API_키_입력"

# ── Hugging Face Hub ──────────────────────────────────────────
# private repo인 경우에만 필요 (public repo면 비워도 됨)
HF_TOKEN = ""

# ── HF Hub 레포 ID ────────────────────────────────────────────
# upload_to_hf.py 실행 시 --repo 인자와 동일하게 입력
HF_REPO_ID = "your-username/kride-models"
